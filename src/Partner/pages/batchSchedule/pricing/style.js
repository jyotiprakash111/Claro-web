import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
  pricingContainer: {
    marginRight: 100,
  },
  pricingContainerDiv: {
    display: "flex",
    alignItems: "center",
  },
  pricingContainerDivP: {
    color: "#FF8021",
  },
  pricingContainerDivIcon: {
    color: "#FF8021",
  },
}));

export default useStyles;
