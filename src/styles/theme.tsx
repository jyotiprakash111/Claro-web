import { createMuiTheme } from "@material-ui/core/styles";

// Creation of a custom theme for this app
const theme = createMuiTheme({
	palette: {
		primary: {
			main: "#FF8021",
		},
		secondary: {
			main: "#65B1EC",
		},
	},
});

export default theme;
